meta {
  name: Create user
  type: http
  seq: 1
}

post {
  url: {{base_url}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "email":"{{new_username}}",
    "password1":"test1",
    "password2":"test2",
  }
}

script:pre-request {
  bru.setVar("new_username", Date.now())
}

settings {
  encodeUrl: true
  timeout: 0
}
